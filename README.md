# WaveX (Wavelet & Frequency Domain Explanations)

This repo contains experiments and tooling around frequency and wavelet explanations.

## Setup

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

## Quick usage (example)

```bash
cd notebooks
```
Run any python notebook with the appropriate python environment active.

